#Lists are basically the data structures and multiple values can be stored in a list
#for example, lets create list called fruits

fruits = ["Mango", "Papaya", "Kiwi", "Orange", "Banana" ]

#now fruits have 5 elements
